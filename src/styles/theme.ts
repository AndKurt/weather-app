import { css, DefaultTheme } from 'styled-components'

// Color palette
const COLOR = {
  WHITE: '#fff',
  BLACK: '#000',
  DARK_BLUE: '#23234C',
}

// Font sizes
const FONT_SIZE = {
  S: '15px',
  M: '32px',
  L: '50px',
  XL: '70px',
}

// Line height
const LINE_HEIGHT = {
  S: '20px',
  M: '40px',
  L: '65px',
  XL: '90px',
}

// Spaces
const SPACES = {
  none: 0,
  '5': 5,
  '15': 15,
  '30': 30,
}

// Width
const WIDTH = {}

// Height
const HEIGHT = {}

export const theme = {
  COLOR,
  SPACES,
  FONT_SIZE,
  LINE_HEIGHT,
}
